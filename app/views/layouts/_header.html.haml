
%nav.round
  %ul
    %li.current_user
      - if current_user
        Eingeloggt als
        = current_user.name
    %li
      = link_to "Home", root_path
    - if signed_in?
      %li
        = link_to "Ausloggen", signout_path, :method => :delete
    - else
      %li
        = link_to "Einloggen", signin_path
%hr